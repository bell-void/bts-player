/* =========================
   BTS PLAYER - ELEGANT CSS
========================= */

*{
  margin:0;
  padding:0;
  box-sizing:border-box;
}

:root{
  --bg:#090011;
  --card:rgba(25,10,45,.75);
  --card2:rgba(45,16,80,.65);
  --purple:#9333ea;
  --purple2:#c084fc;
  --purple3:#7c3aed;
  --text:#f5e9ff;
  --muted:#b89ad8;
  --border:rgba(192,132,252,.18);
}

html,
body{
  height:100%;
  background:
    radial-gradient(circle at top left,#5b21b655 0%,transparent 35%),
    radial-gradient(circle at bottom right,#9333ea33 0%,transparent 35%),
    linear-gradient(135deg,#090011,#140022,#090011);
  color:var(--text);
  font-family:'DM Sans',sans-serif;
  overflow:hidden;
}

/* ===== FONDO ===== */

body::before{
  content:'';
  position:fixed;
  inset:0;
  background:
    radial-gradient(circle,#ffffff11 1px,transparent 1px);
  background-size:40px 40px;
  opacity:.25;
  pointer-events:none;
}

/* ===== APP ===== */

.app{
  position:relative;
  z-index:1;
  display:grid;
  grid-template-columns:260px 1fr;
  grid-template-rows:1fr auto;
  height:100vh;
  max-width:1200px;
  margin:auto;
}

/* ===== SIDEBAR ===== */

.sidebar{
  background:var(--card);
  backdrop-filter:blur(18px);
  border-right:1px solid var(--border);
  padding:24px 16px;
  display:flex;
  flex-direction:column;
  gap:20px;
  overflow-y:auto;
  box-shadow:0 0 25px #00000055;
}

/* ===== LOGO ===== */

.brand{
  display:flex;
  align-items:center;
  gap:12px;
}

.brand-logo{
  width:50px;
  height:50px;
  border-radius:14px;
  display:flex;
  align-items:center;
  justify-content:center;
  background:linear-gradient(135deg,var(--purple),var(--purple2));
  font-family:'Bebas Neue',sans-serif;
  font-size:24px;
  letter-spacing:2px;
  box-shadow:
    0 0 15px #9333ea,
    0 0 35px #9333ea66;
  animation:glow 2s infinite alternate;
}

@keyframes glow{
  from{
    box-shadow:
      0 0 10px #9333ea,
      0 0 25px #9333ea55;
  }
  to{
    box-shadow:
      0 0 20px #c084fc,
      0 0 40px #c084fcaa;
  }
}

.brand-name{
  font-family:'Bebas Neue',sans-serif;
  font-size:28px;
  letter-spacing:3px;
}

.brand-sub{
  font-size:11px;
  color:var(--muted);
  letter-spacing:2px;
  text-transform:uppercase;
}

/* ===== BOTONES ===== */

.nav-section h3{
  font-size:11px;
  color:#8b5fc0;
  margin-bottom:10px;
  letter-spacing:3px;
  text-transform:uppercase;
}

.album-btn{
  width:100%;
  border:none;
  padding:10px 14px;
  border-radius:12px;
  background:rgba(255,255,255,.03);
  color:var(--muted);
  display:flex;
  align-items:center;
  gap:10px;
  cursor:pointer;
  transition:.25s;
  margin-bottom:8px;
}

.album-btn:hover{
  background:rgba(147,51,234,.18);
  transform:translateX(5px);
  border:1px solid #9333ea55;
}

.album-btn.active{
  background:linear-gradient(135deg,#7c3aed,#9333ea);
  color:white;
  box-shadow:0 0 15px #9333ea88;
}

.album-count{
  margin-left:auto;
  background:#2d1050;
  padding:2px 8px;
  border-radius:20px;
  font-size:11px;
}

/* ===== MAIN ===== */

.main{
  display:flex;
  flex-direction:column;
  overflow:hidden;
}

.main-header{
  padding:28px;
}

.era-title{
  font-family:'Bebas Neue',sans-serif;
  font-size:52px;
  letter-spacing:4px;
  text-shadow:0 0 20px #9333ea99;
}

.era-subtitle{
  color:var(--muted);
  margin-top:5px;
}

/* ===== TRACKS ===== */

.tracklist-scroll{
  flex:1;
  overflow-y:auto;
  padding:0 28px 20px;
}

.tracklist-header{
  display:grid;
  grid-template-columns:40px 1fr 170px 60px;
  padding:10px;
  color:#8b5fc0;
  font-size:11px;
  text-transform:uppercase;
  border-bottom:1px solid var(--border);
}

.track-row{
  display:grid;
  grid-template-columns:40px 1fr 170px 60px;
  align-items:center;
  padding:12px;
  border-radius:14px;
  margin-top:8px;
  transition:.25s;
  cursor:pointer;
  border:1px solid transparent;
  background:rgba(255,255,255,.02);
}

.track-row:hover{
  background:rgba(147,51,234,.15);
  border:1px solid #9333ea55;
  transform:scale(1.01);
}

.track-row.playing{
  background:linear-gradient(
    90deg,
    rgba(147,51,234,.25),
    rgba(192,132,252,.08)
  );
  border:1px solid #9333ea99;
  box-shadow:0 0 20px #9333ea44;
}

.track-main{
  display:flex;
  align-items:center;
  gap:12px;
}

.track-art{
  width:42px;
  height:42px;
  border-radius:10px;
  background:var(--card2);
  display:flex;
  align-items:center;
  justify-content:center;
  font-size:20px;
}

.track-title{
  font-size:15px;
  font-weight:500;
}

.track-album-name,
.track-album-tag{
  color:var(--muted);
  font-size:12px;
}

/* ===== PLAYER ===== */

.player-bar{
  background:rgba(15,0,30,.78);
  backdrop-filter:blur(20px);
  border-top:1px solid var(--border);
  padding:16px 28px;
  display:grid;
  grid-template-columns:1fr auto 1fr;
  align-items:center;
  gap:20px;
}

/* ===== NOW PLAYING ===== */

.now-playing-info{
  display:flex;
  align-items:center;
  gap:14px;
}

.now-art{
  width:54px;
  height:54px;
  border-radius:12px;
  background:var(--card2);
  display:flex;
  align-items:center;
  justify-content:center;
  font-size:24px;
}

.now-title{
  font-size:15px;
  font-weight:600;
}

.now-artist{
  color:var(--muted);
  font-size:12px;
}

/* ===== BOTONES PLAYER ===== */

.controls{
  display:flex;
  align-items:center;
  gap:14px;
}

.ctrl-btn{
  border:none;
  background:none;
  color:var(--muted);
  font-size:20px;
  cursor:pointer;
  transition:.2s;
}

.ctrl-btn:hover{
  color:white;
  transform:scale(1.15);
}

.play-btn{
  width:50px;
  height:50px;
  border:none;
  border-radius:50%;
  background:linear-gradient(135deg,var(--purple),var(--purple2));
  color:white;
  font-size:20px;
  cursor:pointer;
  box-shadow:
    0 0 15px #9333ea,
    0 0 35px #9333ea66;
  transition:.25s;
}

.play-btn:hover{
  transform:scale(1.1);
}

/* ===== PROGRESS ===== */

.progress-row{
  display:flex;
  align-items:center;
  gap:10px;
  margin-top:8px;
}

.progress-bar{
  width:340px;
  height:6px;
  background:#2d1050;
  border-radius:20px;
  overflow:hidden;
  cursor:pointer;
}

.progress-fill{
  height:100%;
  width:0%;
  background:linear-gradient(90deg,#9333ea,#c084fc);
  box-shadow:0 0 10px #c084fc;
}

/* ===== VOLUMEN ===== */

input[type=range].vol{
  width:90px;
}

/* ===== SCROLL ===== */

::-webkit-scrollbar{
  width:6px;
}

::-webkit-scrollbar-thumb{
  background:linear-gradient(#9333ea,#c084fc);
  border-radius:20px;
}

/* ===== RESPONSIVE ===== */

@media(max-width:800px){

  .app{
    grid-template-columns:1fr;
  }

  .sidebar{
    flex-direction:row;
    overflow-x:auto;
    overflow-y:hidden;
  }

  .player-bar{
    grid-template-columns:1fr;
    justify-items:center;
  }

  .tracklist-header,
  .track-row{
    grid-template-columns:40px 1fr 60px;
  }

  .track-album-tag{
    display:none;
  }

}